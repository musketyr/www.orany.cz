---
import { getCollection } from 'astro:content';
import BaseLayout from '../layouts/BaseLayout.astro';

const allPosts = await getCollection('blog');
const recentPosts = allPosts
  .sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf())
  .slice(0, 5);

const featuredProjects = [
  {
    name: 'jbx',
    url: 'https://jbx.telegraphic.dev/',
    description: 'Rust-native Java script runner and agent-friendly toolbox for JBang-style workflows',
  },
  {
    name: 'Brick Directory',
    url: 'https://brick.directory/',
    description: 'LEGO parts, sets, and minifig reference directory built for serious AFOL searching',
  },
  {
    name: 'ClawBuddy',
    url: 'https://clawbuddy.help/',
    description: 'OpenClaw mentoring companion with practical skills for building better AI-agent workflows',
  },
  {
    name: 'Pikarama',
    url: 'https://www.pikarama.com/',
    description: 'Group decision app with blind submissions, karma-weighted voting, and memory',
  },
];
---

<BaseLayout title="The Telegraphic Developer — Tech Blog by Vladimír Oraný">
  <div class="container">
    <section class="hero">
      <h1>The Telegraphic Developer</h1>
      <p class="tagline"><span class="blinking-cursor">by Vladimír Oraný</span></p>
      <p class="intro">
        I'm the Telegraphic Developer. I build side projects by Telegram messages any free minute. By day, I'm Principal Software Engineer at Agorapulse, mentoring humans and models.
      </p>
      <div class="hero-links">
        <a href="https://telegraphic.substack.com" target="_blank" rel="noopener" class="hero-link">Newsletter</a>
        <a href="https://www.linkedin.com/in/vladimirorany" target="_blank" rel="noopener" class="hero-link">LinkedIn</a>
        <a href="https://x.com/telegraphic_dev" target="_blank" rel="noopener" class="hero-link">X</a>
        <a href="https://bsky.app/profile/telegraphic.dev" target="_blank" rel="noopener" class="hero-link">Bluesky</a>
      </div>
    </section>

    <section class="recent-posts">
      <h2>Recent Articles</h2>
      <div class="post-list">
        {recentPosts.map((post) => (
          <article class="post-card">
            <div class="traffic-lights"><span class="red"></span><span class="yellow"></span><span class="green"></span></div>
            <time datetime={post.data.date.toISOString()}>
              {post.data.date.toLocaleDateString('en-US', { year: 'numeric', month: 'long', day: 'numeric' })}
            </time>
            <h3>
              <a href={`/blog/${post.slug}`}>{post.data.title}</a>
            </h3>
            {post.data.tags.length > 0 && (
              <div class="tags">
                {post.data.tags.map((tag) => (
                  <span class="tag">{tag}</span>
                ))}
              </div>
            )}
          </article>
        ))}
      </div>
      <a href="/blog" class="view-all">View all articles →</a>
    </section>

    <section class="featured-projects">
      <h2>Featured Projects</h2>
      <div class="projects-grid">
        {featuredProjects.map((project) => (
          <div class="project-card">
            <div class="traffic-lights"><span class="red"></span><span class="yellow"></span><span class="green"></span></div>
            <h3>
              <a href={project.url} target="_blank" rel="noopener">
                {project.name}
              </a>
            </h3>
            <p>{project.description}</p>
          </div>
        ))}
      </div>
      <a href="/projects" class="view-all">View all projects →</a>
    </section>

    <section class="cta-section">
      <div class="cta-card">
        <h2>Got a problem? Let's build the solution.</h2>
        <p>
          Thanks to agentic development, custom apps are now affordable for everyone.
          Tell us what you need — apps start from $50.
        </p>
        <a href="https://ask.telegraphic.dev" class="cta-button" target="_blank" rel="noopener">
          Describe Your Problem →
        </a>
      </div>
    </section>
  </div>
</BaseLayout>

<style>
  .hero {
    text-align: center;
    padding: 4rem 0;
    border-bottom: 1px solid var(--border);
  }

  .hero h1 {
    font-size: 3rem;
    margin-bottom: 0.5rem;
  }

  .tagline {
    font-size: 1.5rem;
    color: var(--accent);
    font-family: 'JetBrains Mono', monospace;
    margin-bottom: 1.5rem;
  }

  .intro {
    font-size: 1.1rem;
    color: var(--text-secondary);
    max-width: 600px;
    margin: 0 auto;
  }

  .hero-links {
    display: flex;
    justify-content: center;
    gap: 1.5rem;
    margin-top: 1.5rem;
  }

  .hero-link {
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.9rem;
    color: var(--text-secondary);
    border: 1px solid var(--border);
    padding: 0.4rem 1rem;
    border-radius: 6px;
    transition: all 0.2s;
  }

  .hero-link:hover {
    border-color: var(--accent);
    color: var(--accent);
  }

  section {
    margin-top: 4rem;
  }

  .post-list {
    display: flex;
    flex-direction: column;
    gap: 2rem;
    margin-bottom: 2rem;
  }

  .post-card {
    background: var(--bg-secondary);
    padding: 1.5rem;
    border-radius: 8px;
    border: 1px solid var(--border);
    transition: border-color 0.2s;
  }

  .post-card:hover {
    border-color: var(--accent);
  }

  .post-card time {
    font-size: 0.9rem;
    color: var(--text-secondary);
    font-family: 'JetBrains Mono', monospace;
  }

  .post-card h3 {
    margin-top: 0.5rem;
    margin-bottom: 0.75rem;
  }

  .tags {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
    margin-top: 0.75rem;
  }

  .tag {
    background: var(--bg-tertiary);
    padding: 0.25rem 0.75rem;
    border-radius: 12px;
    font-size: 0.85rem;
    color: var(--text-secondary);
    font-family: 'JetBrains Mono', monospace;
  }

  .view-all {
    display: inline-block;
    font-family: 'JetBrains Mono', monospace;
    margin-top: 1rem;
  }

  .projects-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 1.5rem;
    margin-bottom: 2rem;
  }

  .project-card {
    background: var(--bg-secondary);
    padding: 1.5rem;
    border-radius: 8px;
    border: 1px solid var(--border);
    transition: border-color 0.2s;
  }

  .project-card:hover {
    border-color: var(--accent);
  }

  .project-card h3 {
    margin-bottom: 0.75rem;
    font-size: 1.1rem;
  }

  .project-card p {
    color: var(--text-secondary);
    font-size: 0.95rem;
    margin-bottom: 0.75rem;
  }

  .stars {
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.9rem;
    color: var(--accent);
  }

  .cta-section {
    margin-top: 4rem;
    margin-bottom: 2rem;
  }

  .cta-card {
    background: linear-gradient(135deg, #0d1117 0%, #161b22 100%);
    border: 1px solid var(--accent);
    border-radius: 12px;
    padding: 3rem;
    text-align: center;
  }

  .cta-card h2 {
    font-size: 1.8rem;
    margin-bottom: 1rem;
  }

  .cta-card p {
    color: var(--text-secondary);
    font-size: 1.1rem;
    max-width: 500px;
    margin: 0 auto 2rem;
  }

  .cta-button {
    display: inline-block;
    background: var(--accent);
    color: #0d1117;
    padding: 0.75rem 2rem;
    border-radius: 8px;
    font-family: 'JetBrains Mono', monospace;
    font-weight: 600;
    font-size: 1rem;
    text-decoration: none;
    transition: opacity 0.2s;
  }

  .cta-button:hover {
    opacity: 0.9;
  }

  @media (max-width: 640px) {
    .hero h1 {
      font-size: 2rem;
    }

    .tagline {
      font-size: 1.2rem;
    }
  }
</style>
